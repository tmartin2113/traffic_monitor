# 511.org API Configuration
# Get your API key at: https://511.org/open-data/token
VITE_511_API_KEY=

# Optional: API Base URL (defaults to https://api.511.org)
VITE_API_BASE_URL=https://api.511.org

# Optional: Polling interval in milliseconds (defaults to 60000 = 1 minute)
VITE_POLL_INTERVAL=60000

# Optional: Cache TTL in milliseconds (defaults to 30000 = 30 seconds)
VITE_CACHE_TTL=30000

# Optional: Maximum API requests per hour (defaults to 60)
VITE_RATE_LIMIT_MAX_REQUESTS=60

# Optional: Enable debug logging (defaults to false in production)
VITE_DEBUG=false

# Optional: Map tile server (defaults to OpenStreetMap)
# VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

# Optional: Enable WZDx data (Work Zone Data Exchange)
VITE_ENABLE_WZDX=false

# Optional: Default map center (Bay Area)
VITE_MAP_DEFAULT_LAT=37.5
VITE_MAP_DEFAULT_LNG=-122.1
VITE_MAP_DEFAULT_ZOOM=10
