# ============================================================================
# 511 Bay Area Traffic Monitor - Environment Configuration Template
# ============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual configuration
# 3. Never commit .env to version control (it's in .gitignore)
# 4. Update .env.example when adding new environment variables
#
# ============================================================================

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# 511.org API Key (REQUIRED)
# Get your free API key at: https://511.org/open-data/token
# This is the primary data source for Bay Area traffic events
VITE_511_API_KEY=your_api_key_here

# ============================================================================
# API CONFIGURATION
# ============================================================================

# API Base URL
# Default: https://api.511.org
# Change only if using a proxy or different endpoint
VITE_API_BASE_URL=https://api.511.org

# API Request Timeout (milliseconds)
# Default: 30000 (30 seconds)
VITE_API_TIMEOUT=30000

# Maximum API Retry Attempts
# Default: 3
VITE_API_MAX_RETRIES=3

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Application Metadata
VITE_APP_TITLE=511 Bay Area Traffic Monitor
VITE_APP_VERSION=3.0.0
VITE_APP_DESCRIPTION=Real-time traffic monitoring for the San Francisco Bay Area

# Environment
# Options: development | staging | production
VITE_APP_ENVIRONMENT=development

# ============================================================================
# DATA REFRESH & CACHING
# ============================================================================

# Polling Interval (milliseconds)
# How often to fetch new data from the API
# Default: 60000 (60 seconds / 1 minute)
# Recommended: 30000-120000 to respect rate limits
VITE_POLL_INTERVAL=60000

# Cache Time-To-Live (milliseconds)
# How long to keep cached data before refetching
# Default: 30000 (30 seconds)
VITE_CACHE_TTL=30000

# Stale Time (milliseconds)
# React Query stale time setting
# Default: 60000 (1 minute)
VITE_STALE_TIME=60000

# ============================================================================
# RATE LIMITING
# ============================================================================

# Maximum Requests Per Hour
# 511.org limit: 60 requests/hour
# Keep this at or below 60 to avoid being rate-limited
VITE_RATE_LIMIT_MAX_REQUESTS=60

# Rate Limit Window (milliseconds)
# Default: 3600000 (1 hour)
VITE_RATE_LIMIT_WINDOW=3600000

# Rate Limit Buffer
# Safety margin to avoid hitting the exact limit
# Default: 100 (milliseconds)
VITE_RATE_LIMIT_BUFFER=100

# ============================================================================
# MAP CONFIGURATION
# ============================================================================

# Default Map Center (San Francisco Bay Area)
VITE_MAP_DEFAULT_LAT=37.7749
VITE_MAP_DEFAULT_LNG=-122.4194
VITE_MAP_DEFAULT_ZOOM=10

# Map Zoom Levels
VITE_MAP_MIN_ZOOM=8
VITE_MAP_MAX_ZOOM=18

# Map Tile Provider
# Default: OpenStreetMap (free)
# Alternative: Mapbox (requires VITE_MAPBOX_TOKEN)
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
VITE_MAP_TILE_ATTRIBUTION=© OpenStreetMap contributors

# Map Update Interval (milliseconds)
# How often to refresh map markers
# Default: 30000 (30 seconds)
VITE_MAP_UPDATE_INTERVAL=30000

# Marker Clustering Threshold
# Number of markers before clustering starts
# Default: 50
VITE_MAP_CLUSTER_THRESHOLD=50

# ============================================================================
# GEOFENCE BOUNDARIES (Bay Area)
# ============================================================================

# Geographic Boundaries for Event Filtering
# These coordinates define the Bay Area monitoring region
VITE_GEOFENCE_NORTH=37.86217361
VITE_GEOFENCE_SOUTH=37.21559028
VITE_GEOFENCE_EAST=-121.66525694
VITE_GEOFENCE_WEST=-122.57031250

# ============================================================================
# FEATURE FLAGS
# ============================================================================

# Enable Work Zone Data Exchange (WZDx) API
# WZDx provides additional construction/work zone data
VITE_ENABLE_WZDX=true

# Enable Marker Clustering
# Groups nearby markers for better performance
VITE_ENABLE_CLUSTERING=true

# Enable Geofencing
# Filters events to Bay Area only
VITE_ENABLE_GEOFENCING=true

# Enable Analytics
# Sends usage data to analytics services
VITE_ENABLE_ANALYTICS=false

# Enable Error Reporting
# Sends errors to monitoring services
VITE_ENABLE_ERROR_REPORTING=true

# Enable Performance Monitoring
# Tracks Web Vitals and performance metrics
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Enable Debug Mode
# Shows additional logging in console
VITE_ENABLE_DEBUG=false

# Enable Beta Features
# Experimental features (use at your own risk)
VITE_ENABLE_BETA_FEATURES=false

# Enable Offline Mode / PWA
# Service worker for offline functionality
VITE_ENABLE_PWA=true

# ============================================================================
# EXTERNAL SERVICES (Optional)
# ============================================================================

# Mapbox (Optional)
# Required if using Mapbox tiles instead of OpenStreetMap
# Get token at: https://account.mapbox.com/access-tokens/
# VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
# VITE_MAPBOX_STYLE_URL=mapbox://styles/mapbox/streets-v12

# Sentry Error Tracking (Optional)
# Get DSN at: https://sentry.io
# VITE_SENTRY_DSN=your_sentry_dsn_here
# VITE_SENTRY_ENVIRONMENT=production
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
# VITE_SENTRY_RELEASE=3.0.0

# Google Analytics (Optional)
# Get tracking ID at: https://analytics.google.com
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Datadog RUM (Optional)
# Get client token at: https://www.datadoghq.com
# VITE_DATADOG_APPLICATION_ID=your_app_id_here
# VITE_DATADOG_CLIENT_TOKEN=your_client_token_here
# VITE_DATADOG_SITE=datadoghq.com
# VITE_DATADOG_SERVICE=511-traffic-monitor
# VITE_DATADOG_ENV=production

# New Relic Browser (Optional)
# Get license key at: https://newrelic.com
# VITE_NEW_RELIC_ACCOUNT_ID=your_account_id_here
# VITE_NEW_RELIC_TRUST_KEY=your_trust_key_here
# VITE_NEW_RELIC_AGENT_ID=your_agent_id_here
# VITE_NEW_RELIC_LICENSE_KEY=your_license_key_here
# VITE_NEW_RELIC_APPLICATION_ID=your_app_id_here

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================

# Development Server Configuration
VITE_DEV_SERVER_PORT=3000
VITE_DEV_SERVER_HOST=localhost
VITE_DEV_SERVER_OPEN=true

# Mock Data (Development Only)
# Use mock data instead of real API calls
VITE_DEV_MOCK_DATA=false

# Hot Module Replacement
VITE_DEV_HMR=true

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================

# Source Maps
# Enable source maps for production builds
VITE_BUILD_SOURCEMAP=false

# Code Minification
VITE_BUILD_MINIFY=true

# Legacy Browser Support
# Build legacy bundles for older browsers
VITE_BUILD_LEGACY=true

# Build Output Directory
VITE_BUILD_OUTPUT_DIR=dist

# Asset Inline Limit (bytes)
# Files smaller than this will be inlined as base64
VITE_BUILD_ASSET_INLINE_LIMIT=4096

# ============================================================================
# PRODUCTION OVERRIDES
# ============================================================================

# These values are typically set in CI/CD or hosting platform
# Examples for Vercel, Netlify, Railway, etc.

# Build Information (Auto-populated by CI/CD)
# VITE_BUILD_TIME=${BUILD_TIME}
# VITE_COMMIT_SHA=${COMMIT_SHA}
# VITE_BRANCH=${BRANCH}
# VITE_BUILD_NUMBER=${BUILD_NUMBER}

# ============================================================================
# NOTES
# ============================================================================

# 1. All variables prefixed with VITE_ are exposed to the client-side code
# 2. Never put sensitive data (API keys, secrets) in production .env files
#    Use your hosting platform's environment variable settings instead
# 3. Keep .env.example updated when adding new variables
# 4. Document all variables with comments for team members
# 5. Use validation in your code to ensure required variables are set
# 6. Consider using different .env files for different environments:
#    - .env.development
#    - .env.staging
#    - .env.production
#    - .env.local (git-ignored, for local overrides)

# ============================================================================
# VALIDATION
# ============================================================================

# Required variables that must be set:
# - VITE_511_API_KEY

# Recommended to configure:
# - VITE_POLL_INTERVAL
# - VITE_CACHE_TTL  
# - VITE_MAP_DEFAULT_LAT
# - VITE_MAP_DEFAULT_LNG
# - VITE_GEOFENCE_* (if monitoring a different region)

# Optional but useful:
# - VITE_SENTRY_DSN (error tracking)
# - VITE_GA_TRACKING_ID (analytics)
# - VITE_MAPBOX_ACCESS_TOKEN (better maps)

# ============================================================================
